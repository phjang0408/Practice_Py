# ============================================================
# 입력
# ============================================================

n = int(input())
a, b = map(int, input().split())
arr = list(map(int, input().split()))

# N줄 입력을 리스트로
arr = [int(input()) for _ in range(n)]

# N줄, 각 줄에 여러 값
arr = [list(map(int, input().split())) for _ in range(n)]

# 빠른 입력 (입력량 많을 때)
import sys
input = sys.stdin.readline


# ============================================================
# 2D 리스트 초기화
# ============================================================

# 0으로 채운 n x m 배열
board = [[0] * m for _ in range(n)]

# 주의: 아래는 같은 리스트를 참조해서 버그 발생
board = [[0] * m] * n   # 절대 사용 금지
board[0][0] = 1         # 모든 행의 [0]이 바뀜


# ============================================================
# 최댓값/최솟값 초기화
# ============================================================

max_val = -float('inf')  # 또는 -1e9
min_val = float('inf')   # 또는 1e9


# ============================================================
# swap
# ============================================================

a, b = b, a  # 임시변수 불필요


# ============================================================
# 리스트 뒤집기
# ============================================================

arr[::-1]           # 새 리스트 반환
arr.reverse()       # 제자리 뒤집기 (반환값 없음)


# ============================================================
# 중복 제거 + 정렬
# ============================================================

arr = [3, 1, 2, 1, 3]
unique = sorted(set(arr))   # [1, 2, 3]


# ============================================================
# 딕셔너리 빈도 카운트
# ============================================================

from collections import Counter
cnt = Counter(arr)      # {값: 개수}
cnt.most_common(1)[0][0]  # 가장 많은 원소


# ============================================================
# 조건부 리스트 (리스트 컴프리헨션)
# ============================================================

evens = [x for x in arr if x % 2 == 0]
squared = [x**2 for x in arr]


# ============================================================
# 문자열 ↔ 리스트
# ============================================================

list("abc")             # ['a', 'b', 'c']
"".join(['a','b','c'])  # "abc"
s[::-1]                 # 문자열 뒤집기


# ============================================================
# 몫과 나머지
# ============================================================

q, r = divmod(17, 5)    # q=3, r=2


# ============================================================
# 삼항 연산자
# ============================================================

result = a if a > b else b  # max(a,b)와 동일


# ============================================================
# 인접한 4방향 / 8방향 탐색
# ============================================================

# 4방향 (상하좌우)
dx = [-1, 1, 0, 0]
dy = [0, 0, -1, 1]

for i in range(4):
    nx, ny = x + dx[i], y + dy[i]
    if 0 <= nx < n and 0 <= ny < m:  # 범위 체크
        pass

# 8방향
dx = [-1,-1,-1, 0, 0, 1, 1, 1]
dy = [-1, 0, 1,-1, 1,-1, 0, 1]


# ============================================================
# 무한 반복 탈출
# ============================================================

while True:
    n = int(input())
    if n == 0:
        break


# ============================================================
# sys.exit 조기 종료
# ============================================================
import sys
if error_condition:
    print("Error")
    sys.exit()  # 이후 코드 실행 안 함